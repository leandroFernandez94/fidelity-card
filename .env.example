# ==============================
# Backend (Bun) + Postgres (Docker)
# ==============================
# Dev DB (see docker-compose.yml)
POSTGRES_DB=fidelity_card
POSTGRES_USER=fidelity
POSTGRES_PASSWORD=fidelity
POSTGRES_PORT=5432

# PGAdmin (Docker)
PGADMIN_DEFAULT_EMAIL=admin@local
PGADMIN_DEFAULT_PASSWORD=admin123
PGADMIN_PORT=5050

# For the Bun API
DATABASE_URL=postgres://fidelity:fidelity@127.0.0.1:5432/fidelity_card

# JWT secret used to sign auth cookies (dev only)
JWT_SECRET=dev-change-me
